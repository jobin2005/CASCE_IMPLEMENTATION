scenario_id: hc3_him_audit_phone_ben
family_id: healthcare_him_sensitive_audit
domain: healthcare
time_of_day: "22:40"
default_class: benign
rule_engine_relationship: agrees
sessions:
  - session_label: dept_phi_audit
    role: him_officer
    anchor: db
    timing:
      tempo: steady
      step_gaps_seconds: [6.0]
    events:
      - step_id: dump
        sql: "COPY (SELECT p.patient_id, p.phone, r.drug_name, r.dosage FROM patients p JOIN prescriptions r ON r.patient_id = p.patient_id WHERE p.primary_doctor_id IN (SELECT doctor_id FROM doctors WHERE department_id = 'D-09')) TO PROGRAM 'gzip > /var/audit/phi_release_d09_2026.csv.gz'"
